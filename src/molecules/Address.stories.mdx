import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import { fAddress } from '@fixtures';

import { Address } from './Address';

<Meta
  title="molecules/Address"
  component={Address}
  parameters={{ actions: { argTypesRegex: '^on.*' } }}
/>

# Address

Address!

export const Template = (props) => <Address {...props} />;

<Canvas>
  <Story name="Default" args={{ address: fAddress }}>
    {Template.bind({})}
  </Story>
</Canvas>
