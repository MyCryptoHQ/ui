import { Test } from './Test';
import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

<Meta title="atoms/Test" component={Test} parameters={{ actions: { argTypesRegex: '^on.*' } }} />

# Test

Test component.

export const Template = (props) => <Test {...props} />;

<Canvas>
  <Story name="Test" args={{ label: 'Foo bar' }}>
    {Template.bind({})}
  </Story>
</Canvas>
