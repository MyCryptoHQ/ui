import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import { Panel } from './Panel';
import { Body } from './Typography';

<Meta
  title="atoms/Panel"
  component={Panel}
  parameters={{ actions: { argTypesRegex: '^on.*' } }}
  argTypes={{
    muted: {
      defaultValue: false,
      control: {
        type: 'boolean'
      }
    }
  }}
/>

# Panel

A simple panel component.

<Canvas>
  <Story name="Default" args={{ children: 'Panel' }}>
    {({ children, ...props }) => (
      <Panel {...props}>
        <Body>{children}</Body>
      </Panel>
    )}
  </Story>
</Canvas>

<Canvas>
  <Story name="Muted" args={{ children: 'Panel', muted: true }}>
    {({ children, ...props }) => (
      <Panel {...props}>
        <Body>{children}</Body>
      </Panel>
    )}
  </Story>
</Canvas>
