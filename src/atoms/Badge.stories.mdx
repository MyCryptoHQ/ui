import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import { Badge } from './Badge';

<Meta
  title="atoms/Badge"
  component={Badge}
  parameters={{ actions: { argTypesRegex: '^on.*' } }}
  argTypes={{
    type: {
      control: {
        type: 'select',
        options: ['success', 'info', 'action', 'warning', 'error', 'clear']
      }
    }
  }}
/>

# Badge

Badges!

<Canvas>
  <Story name="Default" args={{ children: 'Default', type: 'info' }}>
    {(props) => <Badge {...props}>{props.children}</Badge>}
  </Story>
</Canvas>

<Canvas>
  <Story name="Margin" args={{ children: 'Default', type: 'info', ml: '2' }}>
    {(props) => <Badge {...props}>{props.children}</Badge>}
  </Story>
</Canvas>
