import { Meta, Story, Canvas } from '@storybook/addon-docs/blocks';

import { fAddress } from '@fixtures';

import { Blockie } from './Blockie';

<Meta
  title="atoms/Blockie"
  component={Blockie}
  parameters={{ actions: { argTypesRegex: '^on.*' } }}
/>

# Blockie

Blockie!

export const Template = (props) => <Blockie {...props} />;

<Canvas>
  <Story name="Default" args={{ address: fAddress }}>
    {Template.bind({})}
  </Story>
</Canvas>
